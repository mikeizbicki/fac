# About

fac is a make-like build system for llm projects.
The fac.yaml file is like the Makefile and defines a series of targets for fac to build.
facd is a daemon that manages build state and accepts commands.


